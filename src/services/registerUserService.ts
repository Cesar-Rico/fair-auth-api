import { User } from '../models/user';
import { UserInput } from '../types/user';

let users: User[] = [];


export const registerUserService = async (input: UserInput) => {
  //const user = await User.create(input);
  //users.push(user);
  //return user;
};

